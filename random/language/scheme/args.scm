#! /usr/bin/scheme --script

; #!/usr/bin/scheme-r5rs

(display (cdr command-line))

; (define (main arguments) (display (cadr arguments)))

; (define display-args (lambda (argslist) )

; (define a (lambda (b) (display (cdr b)))) ; take user-input and store it in a

;(define funk (lambda (x) (+ x 1)))


; (define (main arguments) (for-each display-file (cdr arguments)) 0)


