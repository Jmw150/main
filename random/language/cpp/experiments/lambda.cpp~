// lambda ish ability in c++

#include <iostream>

int main() 
{
    auto f = [](int x) { return x*42; }; //declaration of lambda function is easy in c++

    std::cout << "f() = " << f(2) << std::endl;
}
