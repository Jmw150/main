/*

incomplete

This is a program that grabs documentation from a file.
Documentation is denoted /** to start and its symmetry to end.
Should work in c and c++.

make allocation for main string as size of file
start and end string like do not need more than 5

*/

#include <stdio.h>
#include <stdlib.h>


int main ()
{


    return 0;
}


char* comments (char* mainStr, char* startStr, char* endStr, int start, int end)
{
    char* comment = (char*) malloc ((end-start)*sizeof(char));
    while (start <= end)
	{
	    if (startStr[0] == mainStr[start])
		  if (//fuck it.. doing this is python
);;;
	}
}
    


    	
