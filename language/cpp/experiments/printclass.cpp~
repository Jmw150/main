#include <iostream>


std::ostream& operator<< (std::ostream &out, dataType const& data) {
    out << data.function() << ':';
    out << data.dat << ':';

    return out;
}


dataType cheese;
std::cout << cheese << '\n';
